from GF2 import one, zero

#a 1 + 1 + 1 + 0

print 'a:', one + one + one + zero  # one
print 'b:', one * one + zero * one + zero * zero + one * one  # zero
print 'c:', (one + one + one) * (one + one + one + one) # zero